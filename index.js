const add= (num1 , num2)=>{
    return num1 + num2;
  }
  
  const substract= (num1 , num2)=>{
      return num1 - num2;
    }
  
    const multply= (num1 , num2)=>{
      return num1 * num2;
    }
  
    const divide= (num1 , num2)=>{
      if(num2 !== 0 ){
      return num1 / num2;
  }
    }